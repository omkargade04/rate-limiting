spring:
  application:
    name: ratelimiter
  datasource:
    url: jdbc:postgresql://localhost:5432/rate-limiting
    username: postgres
    password: Gade$1110$
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  error:
    include-message: always
